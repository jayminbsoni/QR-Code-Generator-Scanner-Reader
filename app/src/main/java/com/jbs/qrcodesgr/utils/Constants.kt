package com.jbs.qrcodesgr.utils

object Constants {

    // Check Thread and VM policy to detect warnings of threads, intent service and ANRs etc.
    // Check Logcat for error details.
    const val IS_STRICT_MODE_ENABLE = false

    /**
     * Default Values
     */
    object DefaultValues {
        const val DEFAULT_STRING = ""
        const val DEFAULT_INT = 0
        const val DEFAULT_BYTE = 0
        const val DEFAULT_LONG = 0L
        const val DEFAULT_FLOAT = 0.0F
        const val DEFAULT_DOUBLE = 0.0
        const val DEFAULT_BOOLEAN = false
    }
}
